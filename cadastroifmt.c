#include <stdlib.h>
#include <stdio.h>
#include "serv.h"
main (){
    
    printf("Programa de cadastro de servidores IFMT\n\n");
    printf("Digite a quantidade de servidores que serão cadastrados: ");
    scanf("%d",&number);

    while(number>count){
        printf("Digite a matrícula: ");
        scanf("%d",&servidor.matricula);
        count++;
    }
}